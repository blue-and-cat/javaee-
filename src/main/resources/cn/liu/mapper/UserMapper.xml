<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapper为映射的根节点-->
<!-- mapper为映射的根节点，namespace指定Dao接口的完整类名
mybatis会依据这个接口动态创建一个实现类去实现这个接口，
而这个实现类是一个Mapper对象-->
<mapper namespace="com.liu.pojo.User">
    <!--id ="接口中的方法名"
　　parameterType="传入的参数类型"
　　resultType = "返回实体类对象，使用包.类名"-->
    <select id="findById" parameterType="int"
            resultType="com.liu.pojo.User">
        select * from users where uid = #{id}
    </select>

    <select id="findAll" resultType="user">
        select  * from users
    </select>
    <insert id="insertUser" parameterType="com.liu.pojo.User">
        insert  into  users(uid,uname,uage) values (#{uid,uname,uage})
    </insert>
    <update id="updateUser" parameterType="com.liu.pojo.User">
        update users set uname=#{uanme},uage=#{uage} where uid=#{id}

    </update>
    <delete id="deleteUser" parameterType="Integer" >
        delete from users where uid=#{uid}
    </delete>


</mapper>

